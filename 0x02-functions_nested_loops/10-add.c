#include <stdio.h>
/**
 * add -function
 * Descreption:adds two integers
 * @d1:the first parameter
 * @d2:the second parameter
 * Return: the addition result
 */
int add(int d1, int d2)
{
	return (d1 + d2);
}
