#include <stdio.h>
#include <stdlib.h>
/**
 * _abs - function
 * Descreption:computes the absolute value of an integer
 * @n: the used number
 * Return: the absolute value of n
 */
int _abs(int n)
{
	return (abs(n));
}

