#include <stdio.h>
#include <string.h>
/**
 * _strlen - function
 * Descreption:returns the length of a string.
 * @s:parameter
 * Return: length of a string
 */
int _strlen(char *s)
{
	return (strlen(s));
}
