#include "main.h"
#include <string.h>
/**
 * _strcpy - function
 * Descreption:copies the string pointed to by src
 * to the buffer pointed to by dest
 * @dest:first par
 * @src:second par
 * Return:the pointer to dest
 */
char *_strcpy(char *dest, char *src)
{
	return (strcpy(dest, src));
}
